<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?android:attr/colorBackground"
        android:orientation="vertical">

        <!-- Status bar scrim: sized to the status bar inset height at runtime -->
        <View
            android:id="@+id/status_bar_scrim"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_alignParentTop="true"
            android:background="?attr/statusBarScrimColor" />

        <!-- Toolbar at top -->
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:layout_below="@id/status_bar_scrim"
            android:background="?attr/colorPrimary"
            android:elevation="4dp"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
            app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
            app:titleTextColor="?attr/colorOnPrimary"/>

        <!-- Quote Counter -->
        <TextView
            android:id="@+id/quote_counter"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/toolbar"
            android:paddingTop="8dp"
            android:paddingBottom="4dp"
            android:text="1 of 545"
            android:textAppearance="@style/TextAppearance.Material3.LabelLarge"
            android:textColor="?android:attr/textColorPrimary"
            android:gravity="center"
            android:textStyle="bold" />

        <!-- ViewPager2 für Swipe-Funktionalität -->
        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/quotes_viewpager"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/quote_counter"
            android:layout_marginTop="4dp" />

    </RelativeLayout>

    <!-- Floating Action Button -->
    <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
        android:id="@+id/fab_add_quote"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:contentDescription="Add quote"
        android:text=""
        app:icon="@android:drawable/ic_input_add"
        app:iconTint="?attr/colorOnPrimary"
        app:backgroundTint="?attr/colorPrimary"
        app:shapeAppearanceOverlay="@style/ShapeAppearance.MyApp.FAB" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>