<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Background color of the status-bar scrim strip drawn behind the transparent status bar
         on edge-to-edge (API 35+) screens. A darker "variant" of colorPrimary so the status bar
         reads as a distinct strip above the toolbar, matching the pre-edge-to-edge
         android:statusBarColor look. See EdgeToEdgeUtils. -->
    <attr name="statusBarScrimColor" format="color" />

</resources>
