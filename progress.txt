2026-09-22 — Drive backup: fixed duplicate uploads from stale local state

Root cause: `BackupRun` decided "unchanged, skip" by comparing against a hash cached locally in `BackupState` (SharedPreferences), written only after `destination.write()` returned. If the process died between the upload succeeding and that local write landing (or any other way the cache fell out of sync with reality), the next run trusted the stale cache and re-uploaded identical content — seen as two Drive files with the same minute-resolution filename.

Fix: `BackupRun` now compares against the destination's own most recent backup (read back and re-hashed) instead of any locally cached value, so it's self-healing regardless of why local state went stale. `BackupDestination` gained `read(Entry)`; `SafDestination` reads the local file, `DriveDestination`/`DriveRestClient` added a `GET .../files/{id}?alt=media` download. `BackupState` dropped the now-unused hash field — it only tracks last-backup time for the Settings display now. `BackupRun.run` dropped its `lastHash` parameter accordingly; `BackupWorker` and tests updated. `BackupRunTest` adds coverage for the crash-recovery case (no local cache, destination still has the same content → skip).

Build and all unit tests pass. On-device verification (watch for a repeat duplicate) deferred to the user.

2026-09-19 — Issue #33: Drive backup no longer skipped after reconnecting with a different account

2026-09-19 — Issue #32: CI workflow; CSV seeding removed so a fresh clone builds

Removed `CsvLoader`, `MainActivity.loadQuotesIfNeeded/loadQuotesFromCsv` (and the `R.raw.zitate` reference and first-launch toast), and `QuotePreferences` (its only keys, `is_first_launch`, had no remaining users). Stored quotes still load via `MyApplication`; with nothing stored the collection is simply empty. `*.csv` stays in `.gitignore`. README: feature list now says JSON export/import, empty-start note updated, CI badge added. New `.github/workflows/android.yml` (push/PR to main, Temurin 17, setup-gradle, `chmod +x gradlew`, `compileDebugJavaWithJavac testDebugUnitTest`, test reports uploaded on failure).

Build and all unit tests pass locally. Green run on GitHub and on-device empty-state check are left to the user/first push. Issue #32 closed.

2026-09-19 — Issue #31: Unit tests for Quote, SuggestionProvider, SourceSuggestions

New `QuoteTest` (favorite toggle/timestamp, recordView, null-as-empty getters, id-only equality/hash, constructor), `SuggestionProviderTest` (author tiers exact/prefix/contains, case-insensitivity, duplicate collapse, count-then-name order, no match; source author/other split, alphabetical order, empty author context, de-duplication) and `SourceSuggestionsTest`. Scaffold `ExampleUnitTest` removed. Short-query gating lives in AddEditActivity (min 2 chars), not the provider, so it is not tested here.

Defect found and fixed: `SourceSuggestions.hasSplit()` returned true whenever author sources existed, even with no other sources, so the adapter added a dangling divider row. It now requires both lists non-empty. Build and all unit tests pass. Issue #31 closed.

2026-09-19 — Issue #30: Notifications enabled flag — permission rule moved into the facade, Settings switch refreshes on resume

`QuoteNotifications.onPermissionResult(context, requestCode, grantResults)` now owns "permission granted, therefore enable": returns null for a foreign request code, else whether notifications are enabled (granting calls `setEnabled(true)`). `MainActivity.onRequestPermissionsResult` only delegates and toasts. `SettingsActivity` keeps the daily switch and its listener as fields and re-syncs the switch in `onResume` via `setDailyNotificationChecked` (listener detached/reattached, same pattern as the Drive switch), so a permission grant on the main screen shows up in Settings. The facade is the only class touching the flag's SharedPreferences.

No new tests: static, Context-bound facade; this is a locality fix in the spirit of ADR-002. Build and all unit tests pass. On-device verification deferred to the user. Issue #30 closed.
